# Server
PORT=3000

# Auth (feature 7)
JWT_SECRET=change-me-in-prod

# AI fix loop (feature 4)
OPENAI_API_KEY=sk-...

# Where the SQLite DB and result zips live (defaults to ./storage)
# STORAGE_DIR=./storage

# Max jobs processed concurrently
# MAX_CONCURRENT_JOBS=2
